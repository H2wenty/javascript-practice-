const obj={
    js:'JavaScript',
    py:'Python',
    rb:'Ruby',
    sql:'struture query language'
}

for (const key in obj) {
//   console.log(`${key} is shortform of ${obj[key]}`);
}

const programming = ['js','py','rb','sql']

for (const key in programming) {
//    console.log(`${key} is index of ${programming[key]}`);   
}

const map = new Map()
map.set('PK','Pakistan')
map.set('TK','Turkey')
map.set('PL','Palestine')

// for (const key in map) {
//   console.log(map);
// }
